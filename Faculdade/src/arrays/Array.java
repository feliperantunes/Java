package arrays;

public class Array {
    static void main(String[] args) {
    // Formas de manipular array
    // Primeira forma: Criamos uma variável e depois atribuimos quantos espaços iremos alocar nela
    int [] primeiroArray; primeiroArray = new int [5];

    // Em seguida informamos qual valor iremos colocar: Informamos o índice e o valor que vai estar alocado
    primeiroArray[0] = 1; primeiroArray[4] = 2;

    // Segunda forma: Criamos um array com os valores já definidos em cada posição
    int [] segundoArray = {7, 4, 2, 1, 0}; }

}